# Dashboard

This folder is reserved for a lightweight alert dashboard.

For the initial project version, the API exposes `/alerts` and `/predict` so a CLI,
web UI, or notebook can consume the same output.
