import os
import sys

# Ensure the repo root is imported first so `scripts` / `models` resolve to this
# project rather than an unrelated package on the global path.
sys.path.insert(0, os.path.dirname(__file__))
